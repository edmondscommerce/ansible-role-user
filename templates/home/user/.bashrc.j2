# .bashrc

##############################################################################
# NOTE - the majority of bash configuration is done in /etc/profile.d/zz_ec.sh

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi
